# Как стабильно запустить сервер

## 🚀 Быстрый старт

```bash
npm start -- --web
```

Затем откройте браузер: **http://localhost:8081**

---

## 🔧 Если сервер упал или не запускается

### 1. Полностью остановить все процессы
```bash
pkill -9 -f "expo"
pkill -9 -f "node.*8081"
```

### 2. Очистить кэш и перезапустить
```bash
npm start -- --web --clear
```

### 3. Если порт 8081 занят
```bash
# Найти процесс
lsof -ti:8081

# Убить процесс (замените PID на номер из предыдущей команды)
kill -9 PID
```

---

## ⚠️ Известные проблемы

### Ошибка "Cannot pipe to closed stream"
**Не критично!** Это просто браузер закрыл соединение. Сервер продолжает работать.

### Ошибка "props.pointerEvents is deprecated"
**Не критично!** Это предупреждение от библиотеки, не влияет на работу.

### Страница недоступна
1. Проверьте что сервер запущен: `lsof -ti:8081`
2. Проверьте ответ сервера: `curl -I http://localhost:8081`
3. Очистите кэш браузера: Ctrl+Shift+R (Cmd+Shift+R на Mac)
4. Откройте консоль браузера (F12) и посмотрите ошибки

---

## 📱 Запуск на мобильных устройствах

### iOS Simulator
```bash
npm start -- --ios
```

### Android Emulator
```bash
npm start -- --android
```

### На реальном устройстве (через Expo Go)
```bash
npm start
```
Затем отсканируйте QR код приложением Expo Go

---

## 🛠️ Дополнительные команды

### Проверить статус сервера
```bash
curl http://localhost:8081
```

### Посмотреть логи в реальном времени
```bash
tail -f /tmp/expo-output.log
```

### Переустановить зависимости (если что-то сломалось)
```bash
rm -rf node_modules
npm install
npm start -- --web --clear
```

---

## ✅ Сервер работает правильно, если видите:

```
Starting project at /Users/evgenia/Downloads/change-client-hub-mobile
Starting Metro Bundler
Waiting on http://localhost:8081
...
Web Bundled [время]ms node_modules/expo-router/entry.js ([число] modules)
LOG  [web] Logs will appear in the browser console
```

После этого можно открывать http://localhost:8081 в браузере!
